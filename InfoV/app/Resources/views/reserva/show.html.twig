{% extends 'menu.html.twig' %}

{% block title %}Infoviajes{% endblock %}

{% block body %}
    {{ parent() }}
<div class="container">
    <div class="cold-md-12">
        <div class="page-header margin-none">
            <h1>Registro exitoso</h1>
        </div>
        <div class="table-responsive">
            <table class="table table-striped table-hover">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reserva.id }}</td>
            </tr>
            <tr>
                <th>Fechareserva</th>
                <td>{% if reserva.fechaReserva %}{{ reserva.fechaReserva|date('Y-m-d') }}{% endif %}</td>
            </tr>
            <tr>
                <th>Numeroasiento</th>
                <td>{{ reserva.numeroAsiento }}</td>
            </tr>
        </tbody>
    </table>
    {% if is_granted("ROLE_ADMIN") %}
    <ul>
        
            <a href="{{ path('reserva_index') }}" class="btn btn-primary">Back to the list</a>
        
            <a href="{{ path('reserva_edit', { 'id': reserva.id }) }}" class="btn btn-primary">Edit</a>
        
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        
    {% endif%}
            </div></div></div>
{% endblock %}
